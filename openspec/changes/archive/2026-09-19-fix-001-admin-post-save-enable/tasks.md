## 1. Admin post save availability

- [x] 1.1 Update the admin post form so client validity updates while required values change, without changing validation rules or submission guards.
- [x] 1.2 Preserve existing slug generation, image, rich-text, category, status, and tag interactions.

## 2. Verification and tracking

- [x] 2.1 Run `npm run tsc`, targeted ESLint for `PostForm`, and `npm run lint`.
- [x] 2.2 Ask the user to run `npm run build` locally; defer browser reproduction/verification until an admin session is available.
